name: lg_ai_voice_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dartssh2: ^2.8.2
  flutter_dotenv: ^5.1.0
  deepgram_speech_to_text: ^2.2.2
  http: ^1.2.2
  record: ^5.1.2
  audioplayers: ^6.0.0
  path_provider: ^2.0.9
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/